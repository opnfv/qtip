name: fake plan
description: fake benchmark plan for demonstration and testing
config:
  facility: local
  engineer: local
  driver: random
  collector: stdout
  reporter: console
QPIs:
  - fake-qpi.yaml
