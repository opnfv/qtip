name: fake plan
description: fake benchmark plan for demonstration and testing
config:
  facility: local
  engineer: local
  driver: sample
  collectors: []
  reporter: console
QPIs:
  - fake-qpi.yaml
