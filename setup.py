#!/usr/bin/env python

from distutils.core import setup


setup(name='qtip',
      py_modules=['qtip'],
      version='1.0',
      author='opnfv',
      packages=['func', 'data.ref_results', 'data.report'])
